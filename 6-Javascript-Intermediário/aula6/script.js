let quadrado = document.querySelector('.quadrado')

function minhaFuncao(evento) {
  console.log(evento)
}

// quadrado.onclick = minhaFuncao // evento no item quadrado

// quadrado.onmousemove = minhaFuncao

// quadrado.onmouseenter = minhaFuncao

// quadrado.onmouseout = minhaFuncao

// window.addEventListener('resize', minhaFuncao)  // evento nna janela

// window.addEventListener('keypress', minhaFuncao)

// window.addEventListener('click', minhaFuncao)